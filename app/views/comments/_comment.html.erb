<div class="media">
  <div class="media-body">
    <%= image_tag comment.user.avatar.variant(resize: '100x100') %>
    <%= comment.body %>

    <p>
    <%= link_to 'Destroy Comment', 
      [comment.article, comment],
      method: :delete,
      data: { confirm: 'Are you sure?' } %>
    </p>
  </div>
</div>
